/**
 * Configurações do sistema
 */
export const Config = {
  // URL da API (nome do serviço Docker)
  API_URL: "http://api:3000",
  
  // Nome da coleção no banco de dados
  COLLECTION_NAME: "playerData",
} as const;

